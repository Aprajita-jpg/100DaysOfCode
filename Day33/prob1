#include <iostream>
using namespace std;

int main() {
    string s = "Hello World";

    for (int i = 0; i < s.length(); i++) {
        if (s[i] >= 'a' && s[i] <= 'z') {   // lowercase to uppercase
            s[i] = s[i] - 32;
        }
    }

    cout << s;
    return 0;
}
